apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  generateName: dp-build-run-
spec:
  pipelineRef:
    name: dp-build-run
  workspaces:
  - name: shared-data
    volumeClaimTemplate:
      spec:
        accessModes:
        - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi
  params:
  - name: repo-url
    value: https://github.com/dp-auto/dpxx-src.git
  - name: image-reference
    value: registry.dev.svc.cluster.local:5000/dp-auto/dp01:0.0.1