apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: hello
  namespace: dp01-ns
spec:
  steps:
    - name: echo
      image: alpine
      script: |
        #!/bin/sh
        echo "Hello World"
